{
  "name": "esteem-catalogue",
  "version": "2.0.0",
  "private": true,
  "description": "Interactive product catalogue for Esteem Multi Systems, Chennai.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && npx --yes serve dist -l 5173"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Lingesvaran-R/Esteem-catalogue.git"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
